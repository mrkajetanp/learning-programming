#include <stdio.h>
#include <ctype.h>
#include <stdlib.h>
#include <string.h>

int main() {

  /* character classification */
  /* isalnum - checks if character is alphanumeric */
  /* isalpha - alphabetic */
  /* islower - lowercase */
  /* isupper - uppercase */
  /* isdigit - digit */
  /* isxdigit - hexadecimal digit */
  /* iscntrl - contol character */
  /* isgraph - graphical character */
  /* isspace - space */
  /* isblank - blank */
  /* isprint - printing */
  /* ispunct - punctuation */

  /* character manipulation */
  /* tolower - converts a character to lowercase */
  /* toupper - to uppercase */

  /* conversions to numeric formats */
  /* atof - byte string to a floating point value */
  /* atoi - to int ; atol - to long ; atoll - to long long  */
  /* strtol ; strtoll - converts a byte string to an integer value */
  /* strtoul ; strtoull - to an unsigned integer value */
  /* strtof ; strtod ; strtold - to a floating point */
  /* strtoimax ; strtoumax - converts a byte string to intmax_t or uintmax_t */

  /* string manipulation */
  /* strcpy ; strcpy_s - copies one string to another */
  /* strncpy ; strncpy_s - copies a certain amount of characters from one string to another */
  /* strcat ; strcat_s - concatenates two strings */
  /* strncat ; strncat_s - concatenates a certain amount of characters of two strings */
  /* strxfrm - transform a string so that strcmp would produce same result as strcoll */
  /* strlen ; strlen_s - length of a given string */
  /* strcmp - compares two strings */
  /* strncmp - compares certain characters of two strings */
  /* strcoll - compares two strings in accordance to the current locale */
  /* strchr - finds the first occurrence of a character */
  /* strrchr - finds the last occurrence of a character */
  /* strspn - returns the length of the maximum initial segment that consists of only the characters found in another byte string  */
  /* strcspn - returns the length of the maximum initial segment that consists of only the characters not found in another byte string */
  /* strpbrk - finds the first location of any character in one string, in another string */
  /* strstr - finds the first occurrence of a substring of characters */
  /* strtok - finds the next token in a byte string */

  /* character array manipulation */
  /* memchr - searches an array for the first occurrence of a character */
  /* memcmp - compares two buffers */
  /* memset - fills a buffer with a character */
  /* memcpy - copies one buffer to another */
  /* memmove - moves one buffer to another */

  /* miscellanous */
  /* strerror ; strerrorlen_s - returns a text version of a given error code */

  return 0;
}











