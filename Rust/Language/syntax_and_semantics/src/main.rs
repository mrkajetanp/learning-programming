mod variable_bindings;
mod functions;
mod primitive_types;
mod ifs;
mod loops;

///! how does it work?

/// some doc comment
/// # another doc comment

fn main() {
    variable_bindings::variable_bindings();
    functions::functions();
    primitive_types::primitive_types();
    ifs::ifs();
    loops::loops();
}
