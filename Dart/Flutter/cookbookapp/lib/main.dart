import 'package:flutter/material.dart';
import "package:cookbookapp/animations.dart";

void main() {
  runApp(MaterialApp(
    home: Page1(),
  ));
}

class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('okk'),
    );
  }
}
